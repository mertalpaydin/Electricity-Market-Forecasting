## Notebooks `06_Final_Evaluation_Quick_Test.ipynb`

- Autogluon evaluation is missing, section names are mixed, also fix the errors.
- Remove the visualization from the notebooks, instead add simple print statement for results
- Execute the file to confirm evaluation is running
- Once confirmed the file is working, implement the same changes to `06_Final_Evaluation.ipynb`

## Commit the repo, create new branch for final run of all files
- In the new branch delete all files generated by the scripts/notebooks. 
There should be only original data file, scripts and notebooks left.
Quick test notebook should also be deleted
- Review all notebooks to set parameters that affect how much data is loaded such `max_samples` to a realistic values
- Review docstrings, comments in scripts and notebooks, revise if necessary

## Final Run
- Give user the bash command to sequentially execute the scripts and notebooks
- Make sure that all notebooks are saved with `_executed` suffix.

## Final Report
- In case final run works, review the output of executed files and prepare final report as a markdown.
- In the final report, not just explain results but try to explain why.
- Also explain the reasoning behind the decisions made such subsampling strategies, utiliziation of masked sMAPE etc.

